import CurrencyFormat from 'react-currency-format';
import { Line } from "react-chartjs-2"; // Graph & chart
import numeral from "numeral"; // fromatting the number
import {Divider,FormControl,Select,MenuItem,Card,CardContent, Typography} from '@material-ui/core';

import { MapContainer as LeafletMap, TileLayer } from "react-leaflet";
import "leaflet/dist/leaflet.css"; // لازم تلزق معاها اللي فوق ده
